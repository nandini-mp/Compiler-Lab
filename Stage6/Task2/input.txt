type
    linkedList {
        int data;
        linkedList next;
    }
endtype

decl
    linkedList head;
    linkedList temp;
enddecl

int main() {
decl
    int n;
enddecl
begin
    head = alloc();
    head.data = 10;
    head.next = null;

    temp = alloc();
    temp.data = 20;
    temp.next = head;

    write(temp.data);
    write(temp.next.data);
    free(head);
    head = alloc();
    head.data = 99;
    write(head.data);

    return 0;
end
}